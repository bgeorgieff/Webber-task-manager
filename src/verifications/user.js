const  loginVerification = (user, password) => {
    let verified = false
  
    if(user === '' || password === '') {
      return verified
    } else {
      return verified = true
    }
  }

export {
  loginVerification
}
